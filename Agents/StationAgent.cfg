AGENT
{
    // The name of your agency
    name = Duna One

    // Description of your agency
    description = Dune One are committed to deploying and maintaining Space Stations and Bases on all worlds in the Kerbol system.

    // Logo URL should be the full path to the image file for the logo (256x160)
    logoURL = ContractPacks/KerbinSpaceStation/Agents/StationLogo

    // Logo URL is the full path to the scaled down image for thumbnails (64x40)
    logoScaledURL = ContractPacks/KerbinSpaceStation/Agents/StationLogoScaled
	mentality = NoRandomContractMentality
}